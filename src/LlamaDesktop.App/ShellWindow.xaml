<Window x:Class="LlamaDesktop.App.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:LlamaDesktop.App.Presentation.Converters"
        Title="Llama Desktop" Width="1180" Height="780" MinWidth="1000" MinHeight="600"
        WindowStartupLocation="CenterScreen" Background="#F5F6F8" FontFamily="Segoe UI">
  <Window.Resources>
    <conv:FileNameConverter x:Key="FileNameConverter"/>
  </Window.Resources>
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid Grid.Row="0" Margin="0,0,0,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="Llama Desktop" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="#374151"/>
      <Button Grid.Column="2" Content="复制 API 地址" Command="{Binding CopyApiCommand}" Margin="0,0,8,0" Padding="12,6"/>
      <Button Grid.Column="3" Content="打开聊天页" Command="{Binding OpenBrowserCommand}" Padding="12,6" Margin="0,0,8,0"/>
      <Button Grid.Column="4" Command="{Binding ToggleSidebarCommand}" ToolTip="{Binding SidebarToggleToolTip}"
              Width="36" Height="32" Padding="0" VerticalAlignment="Center">
        <Path Width="16" Height="16" Stretch="Uniform"
              Stroke="#374151" StrokeThickness="1.5" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
              Data="M5,3 H19 A2,2 0 0 1 21,5 V19 A2,2 0 0 1 19,21 H5 A2,2 0 0 1 3,19 V5 A2,2 0 0 1 5,3 Z M9,3 L9,21"/>
      </Button>
    </Grid>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid x:Name="WebHostGrid"/>
      </Border>
      <Grid Grid.Column="1" x:Name="SidebarContainer" Width="320" Margin="16,0,0,0">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
          <TextBlock Text="GGUF 模型" Margin="0,0,0,4" Foreground="#374151"/>
          <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="ModelPicker" ItemsSource="{Binding Models}"
                      SelectedItem="{Binding SelectedModel, Mode=TwoWay}"
                      MinHeight="32" VerticalContentAlignment="Center"
                      ToolTip="选择要加载的 GGUF 模型">
              <ComboBox.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Converter={StaticResource FileNameConverter}}" ToolTip="{Binding}"/>
                </DataTemplate>
              </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.Column="1" Content="浏览..." Command="{Binding BrowseModelCommand}"
                    MinWidth="72" Margin="8,0,0,0"/>
          </Grid>
        </StackPanel>
        <Button Grid.Row="1" Content="{Binding StartStopLabel}" Command="{Binding ToggleServerCommand}"
                IsEnabled="{Binding CanToggleServer}" Height="40" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="2" Text="{Binding ApiBaseUrl}" TextWrapping="Wrap" Foreground="#5F6368" Margin="0,0,0,8"/>
        <Expander Grid.Row="3" Header="实时日志" IsExpanded="{Binding IsLogDrawerOpen, Mode=TwoWay}">
          <TextBox x:Name="LogText" IsReadOnly="True" AcceptsReturn="True"
                   VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                   Background="#111827" Foreground="#E5E7EB" BorderThickness="0" TextWrapping="NoWrap"/>
        </Expander>
      </Grid>
    </Grid>
  </Grid>
</Window>
